<div class="home_page">
  <button class="add_button"><a href="/clips/new">Add a new clip</a></button>
  <% @clips.each do |clip| %> 

    <ul><div class="index"><a href="/clips/<%= clip[:id] %>"><%= clip[:title] %>:</a> <%= clip[:description] %> by: <%= clip[:performer] %></ul>
  <% end %>
  </div>
</div>